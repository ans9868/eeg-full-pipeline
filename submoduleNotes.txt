TO have logs diffs status ect. from submodule add this command 

git config --global status.submoduleSummary true


To recurslivly clone git module and its submodules use this command 

git clone --recursive remotes/main colleague

Pay attention of detachted head or active branch ... 

if want to update the submodule should ...
git fetch 
git log origin/{branch_name} 
git log --oneline origin/main -10

git checkout -q {SHA1} # -q is so that we don't get a long message about leaving the detachted head 

this does ?? 
git config --global diff.submodule log

# git auto-fetches (submodules) but doesnot auto-update them 

So if ...

git pull 

And the submodule was updated we will see "Submodules changed but not update:"
'''

* vendor/plugins/demo {SHA1} (2):
  < demo-commit #2
  < demo-commit #2

'''

Then should finalize the update with 
git submodule update 
or even better 
git submodule update --init --recursive 

We can 'fix' this whole git pull thing with a little alias 

'''
#! /bin/bash
git pull "$@" &&
  git submodule sync --recursive &&
  git submodule update --init --recursive
'''

'''
chmod +x git-spull
'''

###

Remember to push submodule before container ! 

THere is a configuration setting that can do such that it will on demand push or check for push on submodules 

Something like this 
git config --global alias.spush 'push --recurse-submodules=on-demand'

Quick Cheat Sheet 

Configuration settings

    diff.submodule = log (so you get clearer container diffs when referenced submodule commits changed).
    fetch.recurseSubmodules = on-demand (so you are confident new referenced commits for known submodules get fetched with container updates).
    status.submoduleSummary = true (so git status gets useful again when a referenced submodule commit changed).

Adding or cloning

    Initial add: git submodule add <url> <path>
    Initial container clone: git clone — recursive <url> [<path>]

Grabbing updates inside a submodule

    cd path/to/module
    git fetch
    git checkout -q <commit-sha1>
    cd -
    git commit -am “Updated submodule X to: blah blah”

Grabbing container updates

    git pull
    git submodule sync — recursive
    git submodule update — init — recursive

Updating a submodule inside container code

    git submodule update — remote — rebase — path/to/module
    cd path/to/module
    Local work, testing, eventually staging
    git commit -am “Update to central submodule: blah blah”
    git push
    cd -
    git commit -am “Updated submodule X to: blah blah”

Permanently removing a submodule (1.7.8+)

    git submodule deinit path/to/module
    git rm path/to/module
    git commit -am “Removed submodule X”



###  Generally when updating submodule when pull container repo ... 

git pull
git submodule sync --recursive
git submodule update --init --recursive
